package pusher

// Repeated string literals, extracted to satisfy the goconst linter.
const (
	btcusd                   = "BTCUSD"
	emptyInput               = "empty input"
	emptyString              = "empty string"
	inputTooLong             = "input too long"
	msgShorterInputPadded    = "shorter input padded"
	msgValidHexWithoutPrefix = "valid hex without prefix"
	n0xZZ                    = "0xZZ"
	oddLength                = "odd length"
	solusd                   = "SOLUSD"
)

// Repeated string literals, extracted to satisfy the goconst linter.
const (
	ethusd     = "ETHUSD"
	invalidHex = "invalid hex"
)
