{
  "$id": "/resources/source_config_schemas/monadtokenprice",
  "type": "object",
  "properties": {
    "dataSource": {
      "type": "string",
      "const": "monadtokenprice"
    },
    "rpcEndpoint": {
      "type": "string",
      "description": "Monad RPC endpoint URL"
    },
    "updateFrequency": {
      "type": "string",
      "description": "How often to fetch token prices (e.g. '10s', '1m')"
    },
    "routerAddress": {
      "type": "string",
      "description": "DEX router contract address"
    },
    "tokenAddress": {
      "type": "string",
      "description": "Token contract address"
    },
    "baseTokenAddress": {
      "type": "string",
      "description": "Base token address (e.g., USDC)"
    }
  },
  "required": ["dataSource", "rpcEndpoint", "updateFrequency", "routerAddress", "tokenAddress", "baseTokenAddress"],
  "additionalProperties": false
} 
